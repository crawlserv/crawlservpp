{
	"label": "Filter by query",
	"open": false,
	
	"queries": {
		"type": "array",
		"item-type": "query",
		"query-results": [
			"bool"
		],
		"query-types": [
			"regex"
		],
		"default": [],
		"label": "Keep articles containing:",
		"description": "Articles that do not contain a single word fulfilling one (or all) of these queries will be filtered out.",
		"simple": true,
		"position": 1
	},
	"all": {
		"type": "bool",
		"default": false,
		"label": "All queries must be fulfilled (AND)",
		"description": "Specifies whether articles must contain a word fulfilling all of the above queries instead of only of one of them.",
		"simple": true,
		"position": 2
	}
}